procs:
  db:
    shell: docker run --rm --name sharqia-postgres -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=sharqia -p 5432:5432 postgres:17-alpine
  api:
    shell: uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    cwd: apps/api
